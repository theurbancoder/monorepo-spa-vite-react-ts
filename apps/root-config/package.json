{
  "name": "@example/root-config",
  "scripts": {
    "dev": "vite --port 3005",
    "start": "vite --port 3005",
    "lint": "eslint src --ext js,ts,tsx",
    "format": "prettier --write .",
    "check-format": "prettier --check .",
    "build": "concurrently npm:build:*",
    "build:webpack": "webpack --mode=production",
    "build:types": "tsc"
  },
  "devDependencies": {
    "@my-org/eslint": "workspace:*",
    "@my-org/tsconfig": "workspace:*",
    "@my-org/vite": "workspace:*",
    "eslint": "catalog:eslint",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "typescript": "catalog:typescript"
  },
  "dependencies": {
    "single-spa": "catalog:spa",
    "single-spa-layout": "catalog:spa",
    "vite": "catalog:vite"
  },
  "types": "dist/example-root-config.d.ts"
}
